<div class="columns">
  <% data.forEach(blog => { %>
    <div class="column">
      <h2><%= blog.title %></h2>
      <h4><%= blog.blogger %></h4>
      <figure class="image is-128x128">
        <img src="<%= blog.image %>" />
      </figure>
      <p><%= blog.description %></p>
      <p><%= blog.article %></p>
    </div>
  <% }) %>
</div>
